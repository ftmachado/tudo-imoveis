<form method="post" action="{{ path('imobiliaria_atuacao_delete', {'id': imobiliaria_atuacao.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ imobiliaria_atuacao.id) }}">
    <button class="btn">Delete</button>
</form>
