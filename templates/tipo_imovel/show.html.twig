{% extends 'layout-admin.html.twig' %}

{% block breadcrumb %}TipoImovel{% endblock %}

{% block body %}
    <h1>TipoImovel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_imovel.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ tipo_imovel.nome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_imovel_index') }}">Voltar</a>

    <a href="{{ path('tipo_imovel_edit', {'id': tipo_imovel.id}) }}">Editar</a>

    {{ include('tipo_imovel/_delete_form.html.twig') }}
{% endblock %}
