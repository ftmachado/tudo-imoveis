{% extends 'layout-admin.html.twig' %}

{% block breadcrumb %}Atualizar bairro{% endblock %}

{% block body %}
    <h3>Atualizar bairro</h3>
    {{ include('bairro/_delete_form.html.twig') }}
    <br>

    {{ include('bairro/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-default btn-rounded" href="{{ path('bairro_index') }}">Voltar</a>

{% endblock %}
