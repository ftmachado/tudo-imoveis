{% extends 'layout-admin.html.twig' %}

{% block breadcrumb %}Seu perfil{% endblock %}

{% block body %}
    <h3>{% if app.user %}{{ app.user.nome }}{% endif %}</h3>

<form class="form-horizontal form-padding" method="POST">

<fieldset>
    <legend>Alterar senha</legend>

    {# <div class="form-group form-message-light">
        <label class="col-md-3 control-label" for="password-atual">Senha atual</label>
        <div class="col-md-4">
            <input id="password-atual" name="trocar_senha[password_atual]" type="password" class="form-control old-password" minlength="6" maxlength="20" aria-required="true" required>
        </div>
    </div> #}

    <div class="form-group form-message-light">
        <label class="col-md-3 control-label" for="{{ form.password.first.vars['id'] }}">Senha</label>
        <div class="col-md-4">
            {{ form_widget(form.password.first) }}
        </div>
    </div>

    <div class="form-group form-message-light">
        <label class="col-md-3 control-label" for="{{ form.password.second.vars['id'] }}">Repita a senha</label>
        <div class="col-md-4">
            {{ form_widget(form.password.second) }}
        </div>
    </div>

</fieldset>
    <div>
        <button class="btn btn-primary btn-rounded pull-right">Salvar</button>
    </div>
</form>

{% endblock %}

{% block extrajavascript %}{% endblock %}
